(using_atk_version): 1.1.0

(package):
    (actions):
        (run): ruby install.rb

(project):
    name: The ASDF installer for ATK
    description: This is a package that installs the asdf version manager
    
    (commands):
        (setup): echo 'setup complete! (no setup)' 
    
    (paths):
        project_root: ./